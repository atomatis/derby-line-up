package atomatis.derbylineup.jam

import atomatis.derbylineup.room.entity.Player

class Sorter {


    fun sort(players: Array<Player>): Array<Player>
    {
//        players.sortBy{it.getJamPlayed()}
//        val grouped = players.groupBy { it.getJamPlayed() }
//        grouped.forEach {
//            println(it)
//        }

        return players
    }
}
